{
    "name": "flux/sdk",
    "description": "SDK de PHP para el flux Event Protocol v1 — CloudEvents 1.0 sobre NATS JetStream. Nivel de conformidad L2.",
    "type": "library",
    "license": "MIT",
    "keywords": ["cloudevents", "nats", "jetstream", "event-driven", "flux"],
    "require": {
        "php": ">=8.2",
        "ext-json": "*",
        "psr/log": "^2.0 || ^3.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "suggest": {
        "basis-company/nats": "Cliente de NATS necesario para Flux\\Transport\\BasisNatsTransport. Si usas otro cliente, implementa Flux\\Transport\\NatsTransport.",
        "open-telemetry/api": "Si está presente, el SDK rellena `traceparent` desde el span activo.",
        "ext-mbstring": "No es necesario, pero mejora los mensajes de error con texto multibyte."
    },
    "autoload": {
        "psr-4": {
            "Flux\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Flux\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit"
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "stable"
}
