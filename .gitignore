node_modules/
dist/
build/
*.tsbuildinfo

# Credenciales NATS — nunca versionadas (06-security.md §2)
*.creds
*.nk
*.jwt
.env
.env.*
!.env.example

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# .NET
# .NET: anclados a `sdk-dotnet/` a propósito. Un `bin/` suelto ignora CUALQUIER
# directorio bin del repo a cualquier profundidad, y eso se tragó
# `sdk-rust/src/bin/conformance-harness.rs` sin decir nada: el fichero existía, los
# tests pasaban en local, y simplemente no llegaba al commit.
sdk-dotnet/**/bin/
sdk-dotnet/**/obj/
*.user
TestResults/

# PHP / Composer
vendor/
composer.lock
.phpunit.cache/
.phpunit.result.cache

.DS_Store
Thumbs.db
.idea/
.vscode/

dist-test/

# Rust
target/
Cargo.lock
